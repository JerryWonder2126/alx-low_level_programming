#include "main.h"

/**
 * main - this program tests a function that checks if a number is positive or
 * negative
 *
 * Return: 0 always
 */
int main(void)
{
	int i;

	i = 99;
	positive_or_negative(i);

	return (0);
}
