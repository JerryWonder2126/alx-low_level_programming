#include "main.h"

/**
 * _abs - prints the number in absolute form
 *
 * @n: number to be checked
 * Return: the absolute form of n in integer
 */
int _abs(int n)
{
  if (n < 0)
    {
      n *= -1;
    }
  return (n);
}
