#include <stdio.h>

/**
 * main - this programs prints the name of this script
 *
 * @argc: number of command line arguments
 * @argv: array of command line arguments
 * Return: integer always
 */
int main(int __attribute__((__unused__)) argc, char **argv)
{
	puts(argv[0]);
	return (0);
}
